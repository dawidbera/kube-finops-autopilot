# Production nginx deployment - Autogenerated by KubeFinOps Autopilot
# This manifest is updated by the GitOps bot with optimized resource recommendations
# Original values are replaced with cost-optimized CPU and memory allocations
apiVersion: apps/v1
kind: Deployment
metadata:
  name: nginx
  namespace: prod
spec:
  template:
    spec:
      containers:
      - name: app
        resources:
          # Recommended values calculated from P95 usage metrics
          requests:
            cpu: 144m
            memory: 256Mi
          limits:
            cpu: 144m
            memory: 256Mi
