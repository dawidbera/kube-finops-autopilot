# Sample test application deployment - Autogenerated by KubeFinOps Autopilot
# This manifest is updated by the GitOps bot with optimized resource recommendations
# Original values are replaced with cost-optimized CPU and memory allocations
apiVersion: apps/v1
kind: Deployment
metadata:
  name: test-app
  namespace: dev-test
spec:
  template:
    spec:
      containers:
      - name: app
        resources:
          # Recommended values calculated from P95 usage metrics
          requests:
            cpu: 250m
            memory: 512Mi
          limits:
            cpu: 250m
            memory: 512Mi
