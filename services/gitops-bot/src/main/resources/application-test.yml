# GitOps Bot Test Profile Configuration
# Overrides: Disables SASL authentication for integration testing with Testcontainers
spring:
  cloud:
    stream:
      kafka:
        binder:
          configuration:
            security.protocol: PLAINTEXT  # Test environment uses PLAINTEXT without authentication
            sasl.mechanism: ""
            sasl.jaas.config: ""
